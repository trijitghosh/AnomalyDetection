kafka {
    bootstrap.servers = "localhost:9092"
    schema.registry.url = "http://localhost:8081"
    input.topics.name = [ "agg-stock" ]
    output.topic.name = "anomalous-stock"
}

app {
    application.id = "anomaly-detection"
    anomaly.percentage = 10
}
